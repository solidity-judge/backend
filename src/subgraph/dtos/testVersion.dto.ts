export type TestVersionDto = {
    id: string;
    problem: string;
    version: string;
    gasLimit: string;

    syncingIndex: string;
    block: string;
    timestamp: string;
    txHash: string;
};
